# Data Directory

FinQA dataset files are downloaded here automatically by `notebooks/setup.ipynb`.

- `train_sft.json` — Formatted training data (created by pipeline)
- `val_sft.json` — Formatted validation data (created by pipeline)
- `format_info.json` — Dataset metadata (created by pipeline)
